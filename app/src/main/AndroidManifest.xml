<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apps.trollino">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icon_in_oval"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main_group.CommentActivity"></activity>
        <activity android:name=".ui.main_group.ActivityInPostActivity" />
        <activity android:name=".ui.main_group.PostActivity" />
        <activity android:name=".ui.main_group.SearchActivity" />
        <activity android:name=".ui.main_group.TapeActivity" />
        <activity android:name=".ui.main_group.FavoriteActivity" />
        <activity
            android:name=".ui.authorisation.ForgotPasswordActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".ui.authorisation.RegistrationActivity" />
        <activity android:name=".ui.authorisation.LoginActivity" />
        <activity android:name=".ui.main_group.ProfileActivity" />
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>